<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- 页面标题，会显示在浏览器标签上 -->
  <title>专属祝福</title>
  <style>
    /* 全局样式重置，简单清除默认边距和设置盒模型 */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      /* 设置字体 */
      font-family: "Microsoft YaHei", Arial, sans-serif;
      /* 背景渐变，从粉色到浅蓝色，可自行调整颜色值 */
      background: linear-gradient(to right, #ffd1dc, #e0f7fa);
      /* 让内容垂直水平居中 */
      display: flex;
      justify-content: center;
      align-items: center;
      /* 让页面占满整个视口高度 */
      min-height: 100vh;
    }

    .container {
      /* 白色背景容器 */
      background-color: #fff;
      /* 圆角 */
      border-radius: 10px;
      /* 内边距 */
      padding: 30px;
      /* 盒子阴影，增加立体感 */
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      /* 限制容器宽度，适配不同设备 */
      max-width: 600px;
      /* 让内容在容器内居中 */
      text-align: center;
    }

    h1 {
      /* 标题颜色 */
      color: #e91e63;
      /* 标题字体大小 */
      font-size: 28px;
      /* 标题下方间距 */
      margin-bottom: 20px;
    }

    p {
      /* 段落字体大小 */
      font-size: 18px;
      /* 段落颜色 */
      color: #666;
      /* 行高，让文字更舒展 */
      line-height: 1.8;
      /* 段落之间的间距 */
      margin-bottom: 15px;
    }

    .signature {
      /* 署名样式，右对齐，可根据喜好调整 */
      text-align: right;
      color: #999;
      margin-top: 20px;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>谢谢你，亲爱的小朋友</h1>
    <p>愿你的每一天都阳光灿烂，充满欢笑～</p>
    <p>不管遇到什么困难，都要记得你超勇敢、超厉害呀！</p>
    <p>要一直保持对世界的好奇，去探索更多美好～</p>
    <div class="signature">
      <p>—— 爱你的人</p>
    </div>
  </div>
</body>

</html>
